/**
 * application — usecases and the FurrowPort interface.
 * May depend on domain only. TanStack Query hooks orchestrating the port
 * live here (FurrowPort arrives with the adapter task).
 */
export {};
