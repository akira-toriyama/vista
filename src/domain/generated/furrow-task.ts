/**
 * GENERATED by scripts/codegen-furrow-types.mjs — do not edit.
 * Source of truth: `furrow schema task` (https://raw.githubusercontent.com/akira-toriyama/furrow/main/docs/schema/furrow.task.v2.json).
 * Regenerate with: pnpm codegen
 */

/**
 * Schema for one .furrow/tasks/<id>.json shard (a single task's metadata). The board-wide schema_version lives in .furrow/meta.json, never in a shard. v2 adds the required repos set (owner/repo identifiers; [] = draft, attached to no repo). Pin to a tagged URL or vendor this file.
 */
export interface FurrowTaskShardV2 {
  /**
   * frozen id; == the shard filename stem and bodies/<id>.md stem
   */
  id: string;
  title: string;
  /**
   * a lane from config.toml [lanes].order
   */
  status: string;
  /**
   * sparse 10-step ordering key
   */
  priority: number;
  /**
   * optional coarse 1..5 value (importance) estimate; absent = unset
   */
  value?: number;
  /**
   * optional coarse 1..5 effort (cost) estimate; absent = unset
   */
  effort?: number;
  /**
   * free-form tags (labels are NOT repos; see repos)
   */
  labels: string[];
  /**
   * repositories this task relates to, owner/repo form, sorted+deduped; [] = draft (no repo yet)
   */
  repos: string[];
  parent?: string;
  deps: string[];
  refs: string[];
  checklist: ChecklistItem[];
  created: string;
  updated: string;
  closed: string | null;
  /**
   * when a human last reviewed this task (furrow review <id>); null = never. Tracked separately from updated.
   */
  reviewed: string | null;
  /**
   * relative path, e.g. bodies/t-0042.md
   */
  body: string;
  /**
   * work-item type (e.g. task, epic) — a value from config.toml [types].order; absent = the configured default (normally task). Containers (epics) are skipped by furrow next. Optional (not required): absent is the common case.
   */
  type?: string;
  [k: string]: unknown;
}
export interface ChecklistItem {
  text: string;
  done: boolean;
}
