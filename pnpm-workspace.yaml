# exact-pin deps (no ^/~). pnpm >= 10 reads this here, NOT from .npmrc's
# save-exact — so `pnpm add` pins the exact version. `pnpm check:deps` gates it.
saveExact: true

allowBuilds:
  esbuild: true
  unrs-resolver: true
